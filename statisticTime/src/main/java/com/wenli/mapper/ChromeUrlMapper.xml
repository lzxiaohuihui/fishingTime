<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenli.mapper.ChromeUrlMapper">

    <select id="findTitleByUrl" resultType="string" parameterType="string">
        select title from urls where url like #{url}
    </select>

</mapper>